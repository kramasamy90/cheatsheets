\section{Git}


\subsubsection*{Configuration and Initialization}

\begin{tabularx}{\linewidth}{lX}
    \texttt{git init}       & Intialize empty repo. Creates \texttt{.git} dir.\\
\hline
   \texttt{git config}      & \\
   \texttt{--global}        & Global scope. \texttt{/.gitconfig}.\\
   \texttt{--system}        & All users. \texttt{/etc/gitconfig}.\\
   Default                  & Repo. \texttt{.git/config}.\\
   \texttt{--list}          & List all the config.\\
   \texttt{--edit}          & Manually edit the config.\\
\hline
\end{tabularx}

\begin{tabularx}{\linewidth}{lX}
                            & \textbf{Common configs.}\\ 
    \texttt{user.name}      & String.\\ 
    \texttt{user.email}     & String.\\
    \texttt{color.ui}       & Boolean: true / false.\\
    \texttt{core.editor}    & String. Eg: vim, gedit.\\
\hline
\end{tabularx}


\subsubsection*{Working with Remotes}

\begin{tabularx}{\linewidth}{lX}
    \texttt{git clone}      & Clone a repo. \\
    \hline
    \texttt{git remote}     & \\
    \texttt{-v}             & Show remote repo linked to local repo.\\
                            & Also see: \texttt{git branch -vv}.\\
    \texttt{add}            & Add a remote repo. Eg:\\
                            & \texttt{git remote add <path/to/remote/repo>}. \\
    \texttt{rm}             & Remove remote repo. 
                              Eg: \texttt{git remote rm origin}.\\ 
    \hline
    \texttt{git fetch}      & Get info regarding changes in the remote.\\
                            & \texttt{git fetch origin}.\\ 
    \texttt{git pull}       & Pull changes from remote branch.\\
                            & Eg: \texttt{git pull origin feature}.\\
    \hline
    \texttt{git push}       & Push changes from remote branch.\\
                            & Eg: \texttt{git push origin feature}.\\
    \texttt{-u}             & Push a new branch to remote 
                              \textit{and set upstream tracking}.\\
    \texttt{--force}        & Necessary when the history differs between origin
                              and local, eg. after \texttt{--amend} or 
                              \texttt{git reset}.\\
                            & Safer: \textcolor{magenta}{--force-with-lease}:
                                Pushes only if no one else has  updated the 
                                branch.\\
    \hline
\end{tabularx}

\textcolor{red}{git submodule}


\subsubsection*{Basic Workflow.}

\textbf{Common themes:}\\

\begin{itemize}
\item \texttt{.} (dot) represents all files.\\
        Eg: \texttt{git add .} stages all the files.\\
\item \texttt{--} Names following this list the target 
        file(\textcolor{red}{s}).\\
        Eg: \texttt{git log -- foo1.txt}\\
\item \texttt{-p} Patchwise option for making changes.\\
        \begin{tabular}{l|l}
        \texttt{y} / \texttt{n} & Stage or do not stage the hunk.\\
        \texttt{j} / \texttt{J} & Go to next hunk (staged \texttt{j}).\\
        \texttt{k} / \texttt{K} & Go to previous hunk (staged \texttt{k}).\\
        \texttt{s} & Split the hunk.\\
        \texttt{/} & Search for a hunk matching a regex.\\
        \texttt{q} & Quit without staging remaining hunks.\\
        \end{tabular} \\
        Applies with \texttt{git add}, \texttt{git stash},
        \texttt{git restore}.\\
        \textcolor{red}{TODO:} What are other command with '-p' option?
\end{itemize}


\vfill\null
\columnbreak

\begin{tabularx}{\linewidth}{lX}
   \hline 
\end{tabularx}


\begin{tabularx}{\linewidth}{lX}
\texttt{git add}    & Start tracking a file or stage a file for commit.\\
\texttt{-f}         & To stage a file not tracked,
                         i.e. a file in \texttt{.gitignore}.\\
\hline
\texttt{git commit}     & Commits all staged files to repository. \\
\texttt{--amend}        & Amend to last commit.\\
                        & Eg: \textit{Deleting a file from previous commit.}\\
                        & \quad \texttt{git reset --mixed}\\
                        & \quad \texttt{git add <selected-files>}\\
                        & \quad \texttt{git commit --amend}.\\
                        & NOTE: Also useful to add files to last commit and edit
                            the latest commit message.\\
\texttt{-a}             &  Commit all tracked files.\\
\texttt{-m "..."} & Message is mandatory. If there is no message, git opens text editor to input message. Default text editor can be specified in git-config.\\
\hline
\texttt{git reset}      & Undo commit.\\
                        & Eg: \texttt{git reset --soft HEAD\^}\\
                        & \begin{tabular}{l|l}
                            \texttt{--soft}     & Keep changes on stage.\\
                            \texttt{--mixed}    & Keep changes unstaged.\\
                            \texttt{--hard}     & Delete changes.
                          \end{tabular}\\
\hline  

\texttt{git revert}     & Eg: \texttt{git revert <c1>}. Undo changes introduced
                          after \texttt{<c1>}, but keep the commits and create
                          a new commit.\\
\hline  

\texttt{git restore}    & Undo changes in the tracked but not staged files.\\
\texttt{--staged}       & Unstage staged changes.\\
\hline  

\texttt{git clean}      & To remove untracked files / directories.\\
\texttt{-n}             & Dry run.\\
\texttt{-f}             & \textit{force} Remove only untracked files.\\
\texttt{-fd}            & Remove untracked files and directories.\\
                        & NOTE: \texttt{git clean} requires \texttt{-f} option.\\
                        & Without \texttt{-f} no files are deleted. \\
\hline


\texttt{git stash}      & Stash changes with default message.\\
\texttt{-u}             & Stash untracked changes.\\
\texttt{push}           & \texttt{-m} : Stash message.\\
                        & \texttt{--keep-index} : Stash only unstaged changes.\\
\texttt{apply}          & Apply a stash but do not remove it from the stash.\\
\texttt{drop}           & Drop a stash from the stack.\\
\texttt{pop}            & Apply a stash and remove it from the stack.\\
\texttt{show}           & List the files in a stash.\\
\texttt{show -p}        & List the changes in the files in the stash.\\
\texttt{list}           & List all the stashes.\\
\texttt{clear}          & Drop all the stash.\\
\texttt{branch}         & Create a branch from a stash.\\
                        & Eg: \texttt{git stash branch b1 stash\@{3}}.\\
\hline

\end{tabularx}


\vfill\null
\columnbreak

\subsubsection*{Branching and Navigation}
\begin{tabularx}{\linewidth}{lX}
    \texttt{git branch}     & Create a branch.\\ 
                            & \texttt{git branch <branch-name>}\\
                            & \textcolor{violet}{
                                \texttt{git branch <branch-name> <commit>}}\\
                            & \texttt{<commit>} is an existing commit. 
                                It could be and unreachable commit.\\
    \texttt{-f}             & \texttt{git branch -f b1 <commit>}.\\
                            & Force a branch to point to a target commit.
                                \texttt{<commit>} could be another branch.\\
    \texttt{-a}             & List all branches. A.k.a - \texttt{--all}\\
    \texttt{-m}             & Rename a branch. \\
                            & Eg: \texttt{git branch -m <new-name>}
                                Rename current branch to 
                                \texttt{new-name}.\\
                            & Eg: \texttt{git branch -m old new}\\
    \texttt{-d}             & Aka: \texttt{--delete}. Delete merged branches.\\
    \texttt{-D}             & Force delete a branch.\\
    \texttt{-u}             & Set upstream tracking.\\
                            & Eg: \texttt{git branch -u origin/b1 b1}.\\
    \texttt{-vv}            & Show local branches and corresponding remotes.\\
\hline
    \texttt{git tag}        & \texttt{git tag <tag> <commit>}.\\
                            & \texttt{git tag <tag>}.\\
    \texttt{-a}             & Annotated tag.\\
                            & \texttt{git tag -a <tag> <commit> -m "..."}.\\
    \texttt{-d}             & Delete a tag.\\
    \texttt{-l}             & List all tags.\\
    \texttt{--edit / -e}    & Edit tag annotation.\\
\hline
    \texttt{git merge}      & \texttt{git merge <other-branch>} Merge other to 
                                current.\\
                            & \texttt{git merge b1 b2}: Merge \texttt{b1} to 
                                \texttt{b2}.\\
    \texttt{--abort}        & Abort a merge in progress.\\
\hline
    \texttt{git rebase}     & Apply changes from a branch to the target.\\
                            & Eg: \texttt{git rebase master}: Apply changes 
                                in current branch on top of master.\\
                            & Eg: \texttt{git rebase master dev}: Apply changes
                                in dev on top of master.\\
                            & Eg: \texttt{git rebase master <commit> dev}.\\
                            & Eg: \texttt{git rebase master <from> <to>}.\\
    \texttt{--abort}        & \\
\hline
    \texttt{git checkout}   & Navigate to a commit.\\
    \texttt{-}              & Checkout to previous commit. \\
                            & Eg: \texttt{git checkout -}\\
    \texttt{-b}             & Create a new branch checkout to it. Equivalent to
                               \texttt{git branch} and \texttt{git checkout}. \\
    \texttt{-- <file>}      & Restore a file from a commit.\\
                            & Eg: \texttt{git checkout branch1 -- foo.txt}. \\
                            & Eg: \texttt{git checkout commit1 -- foo.txt}. \\
\hline

\end{tabularx}



\vfill\null
\columnbreak


\subsubsection*{Inspection tools}

\begin{tabularx}{\linewidth}{lX}
\hline
    \texttt{git status}     & \\
    \texttt{-s}             & Short format.\\
\hline
    \texttt{git diff}       & Diff between working state and [default].\\
                            & Eg: \texttt{git diff <old> <new>}.\\
                            & Eg: \texttt{git diff HEAD} : HEAD vs working.\\
                            & Eg: \texttt{git diff <commit>} :
                                            \texttt{<commit>} vs working.\\
    \texttt{--staged}       & Diff between staged and HEAD.\\
    \texttt{--cached}       & Same as \texttt{--staged}.\\
    \texttt{--name-only}    & List only the diff file names.\\
    \texttt{-- }            & Diff for specific files.\\
                            & Eg: \texttt{git diff -- foo.txt}.\\
\hline
    \texttt{git log}        & Show commit history.\\
    \texttt{-nx}            & Show last \texttt{x} commits. \\
                            & \texttt{n} can be ignored. Eg: \texttt{-10}.\\
    \texttt{--oneline}      & Condense message to oneline, shorten hash.\\
    \texttt{--graph}        & Show as graph.\\
    \texttt{--decorate}     & Display tags and branch names.\\
    \texttt{--all}          & Show all branches, \textcolor{red}{and stash?}.\\
    \texttt{--branch}       & Show all local branches. No remote and stashes.\\
    \texttt{--remotes}      & Show all remotes branches,
                               \textcolor{red}{and no local?}\\
\hline
    \texttt{git fsck}       & File system check.\\
    \texttt{--unreachable}  & List all unreachable commits.\\
\end{tabularx}


\subsubsection*{Selected Commands}
\begin{itemize}
\item \texttt{git rev-parse}: Show commit hash.\\
        Eg: \texttt{git rev-parse HEAD}.\\
        Eg: \texttt{git rev-parse <commit>}.\\
        \texttt{--short} : Display shorter hash.\\
        \texttt{--abbrev-ref} : Display branch name.\\
\item \texttt{git check-ignored <file>}: Check whether the file is excluded.\\
        \texttt{git check-ignore -v <file>}:
            \textit{Verbose} - also tells which pattern in \texttt{.gitignore}
            causes the file to be excluded.\\
\item \textbf{Files}: Use \texttt{git rm}, \texttt{git mv}; instead of 
        \texttt{rm} and \texttt{mv}.\\
\item \texttt{git ls-tree}: List cont
\end{itemize}

\textbf{\textcolor{red}{TODO:}} 
\textcolor{red}{\texttt{git ls-tree}}.\\
\textcolor{red}{\texttt{cherry}}. \\
\textcolor{red}{\texttt{git remote prune origin}}.\\
\textcolor{red}{git rm --cached}.\\
\textcolor{red}{git check-ignore -v <file>}.\\
\textcolor{red}{git reflog}.\\
\textcolor{red}{git config with an example config with alias}. \\
\textcolor{red}{Aliases like HEAD, HEAD\^, HEAD@\{1\}, stash@\{1\}}.\\
\textcolor{red}{squash??}.\\
\textcolor{red}{Patch file}.\\
\textcolor{red}{git switch}.\\
\textcolor{red}{git show}.\\
\textcolor{red}{Resolving merge conflict in terminal.}\\
\textcolor{red}{git blame}.\\
\textcolor{red}{git stash -p and -a}.\\
\columnbreak

\subsubsection*{GitHub}
\textbf{Authentication using SSH and GPG keys.}
\begin{enumerate}
\item Add the public keys to your GitHub account.\\
\item Change remote URL to use SSH instead of HTTPS.\\
\item Change \texttt{.gitconfig} to use GPG scheme for commits:\\
        \texttt{git config --global user.signingKey <key\_id/email>}\\
        \texttt{git config --global user.commit.gpuSign true}\\
\item Change the remote repo link to use SSH instead of HTTPS:\\
        \texttt{git remote set-url origin git@github.com:username/repository.git}
\end{enumerate}


\subsubsection*{Gitignore}

Store patterns in \texttt{.gitignore} file.\\
Patterns file/directory paths with matching pattern will be exculded from tracking.\\

\textbf{Patterns}\\
\begin{tabularx}{\linewidth}{lX}
    \texttt{!}              & Reinclude an excluded file. \textcolor{red}{See Note below.}\\
    \texttt{/}              & Directory separator.\\
    \texttt{*}              & Match anything except \texttt{/}.\\
    \texttt{?}              & Match any single character except \texttt{/}.\\
    \texttt{**}             & Match anything including \texttt{/}.\\
    \texttt{\textbackslash} & Escape character.\\
    \hline    
\end{tabularx}

\textcolor{red}{NOTE:} Files whose parent directory has been excluded will not be reincluded.\\
Eg:\\


\begin{itemize}
\item Pattern without a slash:\\
        Match any file / directory with that name.\\
        Eg: \texttt{foo} matches with the file/directory: \texttt{/bar/foo}, \texttt{foo}
\item Pattern ending with a slash:\\
        Matches only with directories.\\
        Eg: \texttt{foo/} Does match a file.\\
\item Pattern with slash in the beginning or middle:\\
        Matches with path relative to \texttt{.gitignore}\\
        Eg: \texttt{/bar/foo} does not match \texttt{/foo} or \texttt{/tmp/bar/foo}\\
\end{itemize}


\begin{mdframed}[backgroundcolor=gray!10,linecolor=Firebrick4]
\begin{verbatim}
$ mkdir foo && touch foo/tmp{1..5}.txt 
----
/.gitignore
foo/*
!tmp1.txt # tmp1.txt will be tracked.
----
/.gitignore
foo
!tmp1.txt # tmp1.txt will not be tracked.
\end{verbatim}
\end{mdframed}
\textcolor{red}{NOTE:} There is no gap after \texttt{!}.\\



\vfill \null
\pagebreak

